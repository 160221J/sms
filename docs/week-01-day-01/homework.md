# Homework — Week 1, Day 1

Due: **before the next session** (instructor: fill date _______________).

Submit in the **same GitHub repo** as `hello.go`. No zip files, no WhatsApp screenshots-only.

## 1. `why-go.md`

Copy [why-go.template.md](why-go.template.md) to `why-go.md` in your repo. Write **in your own words**, about **half a page** (120–200 words).

Answer:

- What is the difference between programming and software engineering?
- Why is Go a good fit for the Student Management System we will build from week 4?
- Name two toolchain commands you used today and what they are for.

If you cannot explain a sentence without the slides open, delete it and write a shorter one you can defend.

## 2. `go version` proof

In the terminal:

```bash
go version
```

Either:

- save a screenshot as `go-version.png` in the repo, **or**
- paste the exact output into the bottom of `why-go.md` in a fenced code block.

It must show **go1.25**.

## 3. Optional stretch

Change the printed message to include your name, `gofmt`, commit, push. Commit message example: `Personalise hello message`.

## How it is checked

| Requirement | Pass |
| --- | --- |
| Repo URL sent | Yes |
| `hello.go` runs | `Hello, Epic Learn` (or your name) |
| No compiled `hello` binary in the repo | `.gitignore` working |
| `why-go.md` is your words, not a paste from ChatGPT you cannot explain | Oral question next class |
| `go1.25` visible | Screenshot or pasted output |
