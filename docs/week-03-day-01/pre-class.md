# Pre-class (send 2–3 days before Week 3)

Copy, fill the date/room, send on WhatsApp / email.

---

**Epic Learn — Go Programming Master Course — Week 3, Day 1**

Bring a **green Week 2 test**. Today is dense: control flow, slices, maps, and two labs.

1. `cd` into your `names` folder and run `go test`. It must pass, including the `TrimSpace` cases.
2. Bring `exercises.md` from Week 2. I will cold-call zero values and `len("café")`.
3. Laptop + charger. Same setup: **Ubuntu** lab, or **WSL2 Ubuntu** on Windows — not PowerShell.
4. Optional reading: [Tour of Go — Flow control](https://go.dev/tour/flowcontrol/1) and [More types: slices, maps](https://go.dev/tour/moretypes/1). Stop before methods.

**Today’s goal:** `if` / `switch` / `for` / `range`, slices (including the backing-array gotcha), maps + comma-ok, and helpers `Contains`, `Unique`, `Freq` with `go test`. The Student Management System still starts in **week 4**.

See you at [DATE] [TIME] [ROOM].  
Umesh Indrajith · Epic Learn Education · 074 1200 659
